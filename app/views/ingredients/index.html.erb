

  <h2><strong>Ingredients</strong> <% if current_user %><%= link_to "(+)", new_ingredient_path %><% end %></h2>

  <div class="ingredient-index">
    <% @ingredients.each do |ingredient| %>
    <p class="ingredient-name">
      <%= link_to ingredient.name, ingredient %>
    </p>
    <% end %>
  </div>

</div>
